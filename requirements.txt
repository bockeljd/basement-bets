fastapi==0.103.1
uvicorn
psycopg2-binary
requests
python-dotenv
beautifulsoup4
python-jose
google-generativeai
python-dateutil

# NOTE: keep Vercel serverless bundle small (< 250MB).
# lxml, numpy, pandas, scikit-learn, scipy are TOO LARGE for Vercel.
# Only use pure python or small libs for deployment.
# Historical data/ is excluded via .vercelignore.

# MLB Model (local development only — too big for Vercel)
# Install separately: pip install pybaseball
# pybaseball  # FanGraphs + Statcast data (requires pandas/numpy)
